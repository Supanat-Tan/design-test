{
    "builds": [
      {
        "src": "src/scss/main.scss",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist"
        }
      }
    ],
    "outputDirectory": "dist"
  }